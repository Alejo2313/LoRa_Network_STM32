{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "openocd",
            "type": "shell",
            "command": "openocd -f ${workspaceFolder}/build/default.cfg -f ${workspaceFolder}/build/stm32l0x.cfg",
            "isBackground": true,
            "dependsOn": "build"
        }
    ]
}